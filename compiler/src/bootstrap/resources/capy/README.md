This bootstrap resource directory intentionally contains no linked stdlib modules.
It lets self-hosting smoke compilation avoid loading bundled libraries from the
compiler output currently being generated.
