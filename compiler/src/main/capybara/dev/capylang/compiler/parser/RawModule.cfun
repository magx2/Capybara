from /dev/capylang/compiler/parser/SourceKind import { SourceKind, moduleFile }

data RawModule {
    name: String,
    path: String,
    input: String,
    sourceKind: SourceKind
}

fun RawModule.file(): String =
    moduleFile(this.sourceKind, this.path, this.name)
