from Pck1 import { * }
from Pck2 import { * } except { add }

fun calculator(a: int, b: int, op: string) =
    if op == "+"
    then add(a,b)
    else if op == "-"
    then subtract(a,b)
    else if op == "*"
    then multiply(a,b)
    else if op == "/"
    then divide(a,b)
    else empty(a+b)
