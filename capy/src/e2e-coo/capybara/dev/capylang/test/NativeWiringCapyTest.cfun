from /capy/test/Assert import { * }
from /capy/test/CapyTest import { * }
from /capy/lang/Effect import { * }
from NativeWiring import { * }

fun tests(): Effect[TestFile] =
    test_file("/dev/capylang/test/NativeWiring.coo", [
        native_wiring_app_reads_host_provider_through_generated_bootstrap().map(assertion => test("NativeWiringApp reads host provider through generated bootstrap", () => assertion)),
        native_provider_can_be_passed_to_domain_code().map(assertion => test("native provider can be passed to domain code", () => assertion)),
    ])

fun native_wiring_app_reads_host_provider_through_generated_bootstrap(): Effect[Assert] =
    let app <- NativeWiringApp()
    assert_that(app.read()).is_equal_to(12345L)

fun native_provider_can_be_passed_to_domain_code(): Effect[Assert] =
    let clock = system_clock()
    let consumer <- NativeClockConsumer(clock)
    assert_that(consumer.read()).is_equal_to(12345L)
